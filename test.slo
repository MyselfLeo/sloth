# Return the square of @0
define square: num -> num {
    if == @0 1 {
        @return = "hello";
    };
    if ! == @0 1 {
        @return = * @0 @0;
    };
}

define main: num -> num {
    print(square(@0) "\n");
}