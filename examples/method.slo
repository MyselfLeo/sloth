builtin io|print;

# Return the square of the number it is used on
define square for num: -> num {
    @return = * @self @self;
}


define main: num -> num {
    io|square()
    # Both should work
    print(@0:square() "\n");
    print(0:square() "\n");
}