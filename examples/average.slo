builtin io;
builtin strings;

define main: num -> num {
    sum = 0;
    i = 0;
    while < i @0 {
        sum = + sum read("Enter " + 1 i "th number: ").to_num();
        i = + i 1;
    };

    print(/ sum @0 "\n");
}